# 이 저장소 루트에는 Flutter 프로젝트가 없다.
# 빌드 산출물 무시는 template/.gitignore 와 builder/.gitignore 가 담당한다.

.DS_Store
*.log

# thegraph 의 런 상태. 런 중에만 쓰는 캐시이고 영속 기록은 이슈다.
# 언제 지워도 되며 이슈를 다시 읽으면 복원된다 — 리뷰에 닿으면 잡음이다.
.thegraph/
